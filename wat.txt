@name wat
@inputs Percent
@outputs 
@persist C:number Targ T:gtable
@trigger all
interval(1)
if(first()) {
C=1
Targ=475260307
T = gTable("Primes",1)
}
for(I=1,2500) {
    if(!Targ%T[C,number]) {
        concmd("say "+ toString(T[C,number]))
        error(toString(T[C,number]))
    }
    else {
        C++
    }
}
